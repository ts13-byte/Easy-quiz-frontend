import { MinutesSecondsFormatPipe } from './minutes-seconds-format.pipe';

describe('MinutesSecondsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesSecondsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
